.DS_Store

# Публикуем на GitHub только HTML из /docs (GitHub Pages source: main /docs).
# Всё остальное (транскрибации, служебное, md, js, css, json, xlsx и т.п.) остаётся локально.
*
!docs/
!docs/**/*.html
!.gitignore
